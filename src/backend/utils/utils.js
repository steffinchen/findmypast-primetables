/**
 * Returns true for all truthy values,
 * false otherwise.
 * @param val: the value to test
 * @return true if val is a truthy value
 */
function isTruthy(val){
  return !!val;
}

module.exports = {
  isTruthy
};
